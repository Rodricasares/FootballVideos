{

    
    /*
  <select className="select">{data.map(( e)=>(
   
            <option 
                key={e.title} 
                value={e.competition}
              
                onChange={(event) => {
                              setSearch(event.target.value);
                
                          }}>{e.competition}</option>))}
    
   </select>*/
  }
  
  {
    /*
   {search ? search.map((index3, a)=>(<div key={index3}>{a.title}</div>)) : ""}
    */
  }
  